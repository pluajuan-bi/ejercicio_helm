apiVersion: v1
kind: Service
metadata:
  name: service-shadcn-data-table-helm
  namespace: pluaj-dev-dev  # Asegúrate de que coincida con tu Deployment
spec:
  selector:
    app: mi-app-dt-helm
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}        # Puerto del Service
      targetPort: {{ .Values.container.port }}  # Puerto del contenedor
  type: ClusterIP